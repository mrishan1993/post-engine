# Infrastructure

Docker, Terraform, monitoring, and deployment for AMP.

Phase-0: root `Dockerfile` + `docker-compose.yml` (Postgres-ready).  
Target: Redis Streams, Temporal, R2, Prometheus/Grafana as services land.
