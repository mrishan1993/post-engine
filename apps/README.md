# Apps

User-facing surfaces on top of AMP services:

- `api/` — FastAPI gateway
- `dashboard/` — operator UI (QA, opportunities, predictions)
- `admin/` — config, prompt registry, model registry

Phase-0 CLIs (`pipeline`, `trend`, `predict`) remain the primary interface.
